#include <stdio.h>
#include <string.h>
int main()
{
    char name[255];
    printf("Введите Ваше имя: ");
    fgets(username, 255, stdin);
    printf("length = %d\n", strlen(username)); /* debug line */
    name[strlen(username)-1] = '\0'; /* remove the newline at the end */
    printf("Привет %s!\n", username);
    return 0;
}
